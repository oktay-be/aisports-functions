name: Deploy Source Discoverer Function

on:
  push:
    branches:
      - main
      - dev
      - 'feature/**'
    paths:
      - 'source_discoverer_function/**'
      - '.github/workflows/deploy-source-discoverer-function.yml'

  workflow_dispatch:

permissions:
  id-token: write
  contents: read

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Authenticate to Google Cloud (Workload Identity Federation)
      uses: google-github-actions/auth@v2
      with:
        workload_identity_provider: 'projects/${{ vars.GCP_PROJECT_NUMBER }}/locations/global/workloadIdentityPools/${{ vars.WIF_POOL_ID }}/providers/${{ vars.WIF_PROVIDER_ID }}'
        service_account: ${{ vars.GCP_SERVICE_ACCOUNT }}
        export_environment_variables: true

    - name: Deploy Cloud Function
      uses: google-github-actions/deploy-cloud-functions@v3
      with:
        name: source-discoverer
        runtime: python312
        memory: 256Mi
        cpu: 1
        service_timeout: 60
        max_instance_count: 1
        min_instance_count: 0
        max_instance_request_concurrency: 1
        entry_point: source_discoverer
        source_dir: source_discoverer_function
        event_trigger_type: google.cloud.pubsub.topic.v1.messagePublished
        event_trigger_pubsub_topic: projects/${{ vars.GOOGLE_CLOUD_PROJECT }}/topics/${{ vars.SOURCE_DISCOVERY_TOPIC }}
        region: ${{ vars.REGION }}
        environment_variables: |
          GOOGLE_CLOUD_PROJECT=${{ vars.GOOGLE_CLOUD_PROJECT }}
          GCS_BUCKET_NAME=${{ vars.GCS_BUCKET_NAME }}
          ENVIRONMENT=${{ vars.FUNCTION_ENVIRONMENT }}
        service_account: ${{ vars.GCP_SERVICE_ACCOUNT }}
